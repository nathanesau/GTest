name: run_tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest
    
    container: nathanesau/github_actions:qt-static

    steps:
    - uses: actions/checkout@v2
    - name: Running Tests
      run: ls && cd ${GITHUB_WORKSPACE} && mkdir build && cd build && cmake .. && make && test/tests
